<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:mongo="http://www.springframework.org/schema/data/mongo"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd        http://camel.apache.org/schema/spring       http://camel.apache.org/schema/spring/camel-spring.xsd     http://www.springframework.org/schema/data/mongo       http://www.springframework.org/schema/data/mongo/spring-mongo.xsd">
    <mongo:mongo-client
        credentials="${mongo.user}:${mongo.pass}@${mongo.dbname}"
        host="${mongo.url}" id="mongoBean" port="${mongo.port}">
        <mongo:client-options write-concern="NORMAL"/>
    </mongo:mongo-client>
    <camelContext id="camel" xmlns="http://camel.apache.org/schema/spring">
        <route id="_route1">
            <from id="_from1" uri="kafka:${kafka.topic}?brokers=${kafka.brokers}"/>
            <log id="_log1" message=">>>> Escuchando kafka >>>>>>> ${body}"/>
            <to id="_to1" uri="mongodb:mongoBean?database=${mongo.dbname}&amp;operation=insert&amp;collection=${clientes}"/>
            <log id="_log2" message=">>>> After Insert MongoDB >>>>>>> ${body}"/>
            <to id="_to2" uri="mongodb:mongoBean?database=${mongo.dbname}&amp;collection=${clientes}&amp;operation=findAll"/>
            <log id="_log3" message=">>>> FindAll MongoDB >>>>>>> ${body}"/>
        </route>
    </camelContext>
</beans>
